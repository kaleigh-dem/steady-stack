name: Release template

on:
  workflow_dispatch:
    inputs:
      version:
        description: Prepared semantic template version
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      VERSION: ${{ inputs.version }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 10.13.1

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Verify prepared release
        run: pnpm template:release:verify -- --version "$VERSION"

      - name: Ensure release tag is new
        run: |
          if git rev-parse --verify --quiet "refs/tags/template-v${VERSION}"; then
            echo "template-v${VERSION} already exists" >&2
            exit 1
          fi

      - name: Pack workspace plugin
        run: |
          pnpm template:release:pack -- \
            --version "$VERSION" \
            --output "$RUNNER_TEMP/template-release"

      - name: Smoke local release artifact
        run: |
          pnpm template:release:smoke -- \
            --artifact "$RUNNER_TEMP/template-release/steadystack-workspace-plugin-${VERSION}.tgz" \
            --expected-version "$VERSION"

      - name: Generate release notes
        run: |
          pnpm template:release:notes -- \
            --version "$VERSION" \
            --output "$RUNNER_TEMP/template-release/release-notes.md"

      - name: Create draft release
        id: create_release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="template-v${VERSION}"
          gh release create "$tag" \
            "$RUNNER_TEMP/template-release/steadystack-workspace-plugin-${VERSION}.tgz" \
            --target "$GITHUB_SHA" \
            --draft \
            --title "Template ${VERSION}" \
            --notes-file "$RUNNER_TEMP/template-release/release-notes.md"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

      - name: Download draft release artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p "$RUNNER_TEMP/template-release/published"
          gh release download "${{ steps.create_release.outputs.tag }}" \
            --pattern '*.tgz' \
            --dir "$RUNNER_TEMP/template-release/published"

      - name: Smoke published release artifact
        run: |
          pnpm template:release:smoke -- \
            --artifact "$RUNNER_TEMP/template-release/published/steadystack-workspace-plugin-${VERSION}.tgz" \
            --expected-version "$VERSION"

      - name: Validate published artifact in a generated workspace
        run: |
          pnpm template:workspace:e2e -- \
            --artifact "$RUNNER_TEMP/template-release/published/steadystack-workspace-plugin-${VERSION}.tgz" \
            --expected-version "$VERSION" \
            --workspace "$RUNNER_TEMP/generated-workspace"

      - name: Upload generated-workspace diagnostics
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: generated-workspace-release-diagnostics
          path: ${{ runner.temp }}/generated-workspace/test-output
          if-no-files-found: ignore
          retention-days: 7

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release edit "${{ steps.create_release.outputs.tag }}" --draft=false

      - name: Remove failed draft release
        if: failure() && steps.create_release.outcome == 'success'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release delete "${{ steps.create_release.outputs.tag }}" --yes --cleanup-tag
